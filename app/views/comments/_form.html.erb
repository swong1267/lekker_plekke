<%= form_for @comment do |f| %>
	<p>
		<%= f.label :commenter %>
		<%= f.text_field :commenter %>		
	</p>

	<p>
		<%= f.label :text %>
		<%= f.text_area :text %>		
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>

<%= render 'comments/form' %>